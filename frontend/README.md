# AgentAudit Frontend

Streamlit UI for running audits and reviewing results.

## Run
```bash
# From the project root
streamlit run frontend/streamlit_app.py
```

## Notes
- Backend should be running on `http://localhost:8000`.
- The target endpoint must be reachable without authentication in this version.
